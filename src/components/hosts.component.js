import React, { Component } from 'react';

export default class Hosts extends Component {
    render() {
        return (
            <div>
                <p>Event and venue hosts access through this page</p>
            </div>
        )
    }
}